# 作业

## 一、数据预处理

```python
import pandas as pd
import numpy as np

df = pd.read_csv("SSECI.csv")
df['Date'] = pd.to_datetime(df['Date'])
df = df.set_index('Date')

![](original_series.png)

```python
df['log_return'] = np.log(df['SSECI']).diff()
returns = df['log_return'].dropna()
```
![](log_return_series.png)

---

## 二、平稳性判断

```python
from statsmodels.tsa.stattools import adfuller

adf_result = adfuller(returns)
```

---

## 三、白噪声检验

```python
from statsmodels.stats.diagnostic import acorr_ljungbox

lb = acorr_ljungbox(returns, lags=[10], return_df=True)
```

检验结果：

      lb_stat     lb_pvalue
10  52.248284  1.026477e-07

p 值显著小于 0.05，可对该序列简历平稳时间序列模型

---

## 四、模型识别

### ACF 图

![](acf.png)

### PACF 图

![](pacf.png)

二者都拖尾，再通过AIC/BIC比较，用ARMA(2,1)模型。

---

## 五、模型参数的估计

```python
from statsmodels.tsa.arima.model import ARIMA

model = ARIMA(returns, order=(2,0,1))
fit = model.fit()

print(fit.summary())
```

---

## 六、模型充分性检验

```python
res_lb = acorr_ljungbox(fit.resid, lags=[10], return_df=True)
```
结果：

      lb_stat  lb_pvalue
10  18.185167   0.051919
残差序列 p 值大于 0.05，说明残差是白噪声序列，模型拟合较充分。

---

## 七、ARMR模型预测

```python
train = returns[:-20]
test = returns[-20:]

forecast_model = ARIMA(train, order=(2,0,1)).fit()
forecast = forecast_model.forecast(steps=20)
```

![](forecast.png)

预测误差：
RMSE：0.009621
MAE：0.007489
---

## 八、实验结论

ARMA(2,1) 模型能较好地描述SSECI的动态变化特征

