# 作业

## 一、数据预处理
```python
import pandas as pd
import numpy as np

df = pd.read_csv("SSECI.csv")
df['Date'] = pd.to_datetime(df['Date'])
df = df.set_index('Date')

```python
df['log_return'] = np.log(df['SSECI']).diff()
returns = df['log_return'].dropna()
```

---

## 二、平稳性判断
```python
from statsmodels.tsa.stattools import adfuller

adf_result = adfuller(returns)
print('ADF统计量：', adf_result[0])
print('p值：', adf_result[1])
print('临界值：', adf_result[4])
```
p 值小于 0.05，拒绝原假设，序列平稳

---

## 三、白噪声检验
```python
from statsmodels.stats.diagnostic import acorr_ljungbox

lb = acorr_ljungbox(returns, lags=[10], return_df=True)
```

结果：lb_pvalue≈1.026477e-07<0.05
p值显著小于0.05，可对该序列简历平稳时间序列模型

---

## 四、模型识别
```python

from statsmodels.graphics.tsaplots import plot_acf, plot_pacf

plt.figure(figsize=(12,5))
plt.subplot(121)
plot_acf(returns, lags=20, ax=plt.gca())
plt.subplot(122)
plot_pacf(returns, lags=20, ax=plt.gca())
plt.show()

ACF,PACF都拖尾，再通过AIC/BIC比较，用ARMA(2,1)模型

---

## 五、模型参数的估计
```python
from statsmodels.tsa.arima.model import ARIMA

model = ARIMA(returns, order=(2,0,1))
fit = model.fit()
print(fit.summary())
```

---

## 六、模型充分性检验
```python
res_lb = acorr_ljungbox(fit.resid, lags=[10], return_df=True)
print(res_lb)
```
残差p值大于0.05，说明残差是白噪声序列，模型提取信息较为充分

---

## 七、ARMA模型预测
```python
train = returns[:-20]
test = returns[-20:]

forecast_model = ARIMA(train, order=(2,0,1)).fit()
forecast = forecast_model.forecast(steps=20)
```

预测误差：
RMSE：0.009621
MAE：0.007489
---

## 八、实验结论
ARMS(2,1) 模型能较好地描述SSECI的动态特征

